import * as React from 'react';
import { Map } from '../../components/Places/Map';
type Props = {
  
};
export const PlacesPage = (props: Props) => {
  return (
    <div>
      <Map />
    </div>
  );
};